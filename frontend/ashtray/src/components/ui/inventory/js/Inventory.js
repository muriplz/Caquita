class Inventory {
    constructor(id, userId, items, height, width) {
        this.id = id;
        this.userId = userId;
        this.items = items;
        this.height = height;
        this.width = width;
    }
}

export default Inventory;