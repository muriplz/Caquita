class PetitionReply {
    constructor(id, messageId, userId, content, creation) {
        this.id = id;
        this.messageId = messageId;
        this.userId = userId;
        this.content = content;
        this.creation = creation;
    }
}

export default PetitionReply
