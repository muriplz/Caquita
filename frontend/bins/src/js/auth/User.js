class User {
    constructor(id, username, creation, trust) {
        this.id = id;
        this.username = username;
        this.creation = creation;
        this.trust = trust;
    }
}

export default User;