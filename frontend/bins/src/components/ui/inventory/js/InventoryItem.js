export default class InventoryItem {
    constructor(id, cells, orientation, nbt) {
        this.id = id;
        this.cells = cells;
        this.orientation = orientation;
        this.nbt = nbt;
    }
}
